- include_vars: "{{ ansible_facts['os_family'] }}.yml"
  tags: always

- block:
    - import_tasks: install.yml
    - import_tasks: keys.yml
  when: wireguard_phase == 'keys'

- block:
    - import_tasks: config.yml
  when: wireguard_phase == 'config'
- include_vars: "{{ ansible_facts['distribution'] }}.yml"
  tags: always

- block:

  - name: create ansible temp directories
    ansible.builtin.file:
      path: "{{ item }}"
      state: directory
      owner: root
      group: root
      mode: '1777'
    loop:
      - /tmp/.ansible-root
      - "/tmp/.ansible-{{ main_usr }}"

  - import_tasks: system_setup/apt.yml
  - import_tasks: software/autoupdate_OS.yml
  - import_tasks: software/sudo.yml
  - import_tasks: software/python.yml
  - import_tasks: system_setup/users.yml
  - import_tasks: system_setup/ssh.yml
  - import_tasks: system_setup/firewall.yml
  - import_tasks: system_setup/memory.yml

  when: ansible_facts["distribution"] in ["Debian", "Ubuntu"]
  become: true
- name: software | python | Install packages
  tags: python
  ansible.builtin.apt:
    name:
      - "{{ py_package }}"
      - "{{ py_package_mngr }}"
      - "{{ py_venv_package }}"
      - "{{ py_setuptools_package }}"
      - "{{ py_psutil_package }}"
    state: present
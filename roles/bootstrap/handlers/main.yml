---
- name: update apt cache
  tags: apt
  ansible.builtin.apt:
    update_cache: yes

- name: reload_sshd
  tags: ssh
  ansible.builtin.command: systemctl kill -s HUP {{ openssh_service }}
  changed_when: true
---
- name: update apt cache
  tags: apt
  ansible.builtin.apt:
    update_cache: yes

- name: restart_sshd
  ansible.builtin.service:
    name: "{{ openssh_service }}"
    state: reload
  listen: restart_sshd
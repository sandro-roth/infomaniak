---
- name: update apt cache
  tags: apt
  ansible.builtin.apt:
    update_cache: yes

- name: restart_sshd
  ansible.builtin.service:
    name: "{{ openssh_service }}"
    state: reloaded
  listen: restart_sshd
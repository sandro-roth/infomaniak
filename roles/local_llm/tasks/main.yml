- include_vars: "{{ ansible_facts.os_family }}.yml"
  tags: always

- block:

    - import_tasks: system_setup/llama_install.yml

  when: ansible_facts.os_family == 'Darwin'
  become: false

- block:


  when: ansible_facts.os_family == 'Debian'
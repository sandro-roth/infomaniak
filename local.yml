- hosts: all
  tags: always
  become: true
  gather_facts: true
  pre_tasks:
    - name: pre-run | Update apt cache
      ansible.builtin.apt:
        update_cache: yes
      when: ansible_facts["distribution"] in ["Debian", "Ubuntu"]

# roles
